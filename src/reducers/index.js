// import * as actions from '../actions';

const initialState = {
  guesses: [42, 66],
  feedback: "Choose a number!",
  auralStatus: "Here's the aural status",
  correctAnswer: 99
};

export const reducer = (state = initialState, action) => {
  return state;
};